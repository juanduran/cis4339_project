<%= simple_form_for(@crew) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :crew_name %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>

<%= simple_form_for(@crew) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :crew_name %>
    <%= f.input :employee_id %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>




<p id="notice"><%= notice %></p>
<div class="row">
  <div class="col-md-8 col-md-offset-2">
    <div class="panel panel-default">
      <div class ="panel-heading">
        <h3 class="panel-title">Equpiment Checkout</h3>
      </div>
      <div class = "panel-body">
<p>
  <strong>Checkout date:</strong>
  <%= @equipment_checkout.checkout_date %>
</p>

<p>
  <strong>Return date:</strong>
  <%= @equipment_checkout.return_date %>
</p>

        <% if @equipment_checkout.employee.present? %>
<p>
  <strong>Employee:</strong>
  <%= @equipment_checkout.employee.employee_name %>
</p>

            <% end %>

        <% if @equipment_checkout.equipment.present? %>
<p>
  <strong>Equipment:</strong>
  <%= @equipment_checkout.equipment.equipment_name %>
</p>

            <% end %>

      </div>
      <div class="panel-footer">
        <div class="btn-group>">
        <%= link_to 'Edit', edit_equipment_checkout_path(@equipment_checkout), class: "btn btn-danger"  %> 
        <%= link_to 'Back', equipment_checkouts_path, class: "btn btn-warning "%>
      </div>
      </div>
      </div>
      </div>
    </div>


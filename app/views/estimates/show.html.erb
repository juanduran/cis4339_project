
<p id="notice"><%= notice %></p>
<div class="row">
  <div class="col-md-8 col-md-offset-2">
    <div class="panel panel-default">
      <div class ="panel-heading">
        <h3 class="panel-title">Estimates</h3>
      </div>
      <div class = "panel-body">
        <p> 
<p>
  <strong>Potential customer:</strong>
  <%= @estimate.potential_customer %>
</p>

<p>
  <strong>Terms:</strong>
  <%= @estimate.terms %>
</p>

<p>
  <strong>Description:</strong>
  <%= @estimate.description %>
</p>

<p>
  <strong>Total:</strong>
  <% @estimate.totalprice %>
  <%= @estimate.totalprice2 %>
</p>

        <% if @estimate.customer.present? %>
<p>
  <strong>Customer:</strong>
  <%= @estimate.customer.customer_name %>
</p>

            <% end %>

      </div>
      <div class="panel-footer">
        <div class="btn-group>">
          
        <%= link_to "Printable Receipt (PDF)", estimate_path(@estimate, format: "pdf"), class: "btn btn-danger"  %>  
        <%= link_to 'Edit', edit_estimate_path(@estimate), class: "btn btn-danger"  %> 
        <%= link_to 'Back', estimates_path, class: "btn btn-warning "%>
      </div>
      </div>
      </div>
      </div>
    </div>

